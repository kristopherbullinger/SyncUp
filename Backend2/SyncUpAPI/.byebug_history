c
v
event
modifiedEvents
JSON::parse(event.to_json).merge({"attendees" => event.set_attendees})
event
c
modifiedEvents
c
modifiedEvents.to_json
modifiedEvents
c
modifiedEvents.to_json
modifiedEvents << newevent
newevent = JSON::parse(event.to_json).merge("attendees" => event.set_attendees)
modifiedEvents << _
JSON::parse(event.to_json).merge("attendees" => event.set_attendees)
JSON::parse(event.to_json)
JSON::parse(event)
event
c
modifiedEvents.first.to_json
modifiedEvents.first.attendees
modifiedEvents
c
@events.first.attributes.merge({:attendees => @events.first.set_attendees}).to_json
@events.first.to_json
@events.first
@events.first.attributes.merge({:attendees => @events.first.attendees})
@events.first.attendees
@event.first.attendees
@events.first.to_json
@events.first.set_attendees
@events.first.attendances
@events
c
event.attendees
event
event.set_attendees
event
event.to_json
event.attendances
event.to_json
event.attendees
event.tags
c
@events.first.attendees
@events
c
@events.first.attendees = @events.first.users.map {|user| user.name}
@events.first.set_attendees
c
event.attendanees = event.users.map {|user| user.name}
event.attendances = event.users.map {|user| user.name}
event.users
event.attendances
event
c
@events
exit
Event.first
event1 = Event.create(title: "My FUn Event", description: "Come and have some fun now!", date: Time.zone.parse("2018-11-15"))
Attendance.all
@events.first.users
@events.length
@events
c
Event.first.attendances.first
Event.first
@events.first.attendances
@events.first.attendances = @events.first.attendances.map {|att| att.user.name}
@events.first
@events.
    8:       event.attendances = att
    7:       att = event.attendances.map {|attendance|  attendance.user.name}
@events.each do |event|
@events
c
event.attendances = event.attendances.map{|attendance| attendance.username}
@events.first.attendances
@events
